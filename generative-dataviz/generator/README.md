# Moved

The binary-bars generator now lives in
[`public/generative-dataviz/generator/`](../../public/generative-dataviz/generator/),
next to the post it draws for. This note keeps older links to this folder working.
